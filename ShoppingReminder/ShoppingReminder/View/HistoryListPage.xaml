<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ShoppingReminder.View.HistoryListPage">
  <ContentView.Content>
      <StackLayout>
            <ListView ItemsSource="{Binding PurchaseList}" SelectedItem="{Binding SelectedPurchaseList, Mode=TwoWay}"   RowHeight="50">
            <ListView.ItemTemplate>
              <DataTemplate>
                <ViewCell>
                  <ViewCell.View>
                    <StackLayout Orientation="Horizontal">
                      <Label Text="{Binding Date}"/>
                      <Label Text="{Binding Id}"/>
                                    <Button Text="Удалить" Command="{Binding ListVM.DeleteHistoryItemCommand}" CommandParameter="{Binding}"/>
                     </StackLayout>
                  </ViewCell.View>
                </ViewCell>
              </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
      </StackLayout>
  </ContentView.Content>
</ContentView>