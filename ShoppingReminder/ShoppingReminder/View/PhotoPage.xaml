<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ShoppingReminder.Ad"
             xmlns:languages="clr-namespace:ShoppingReminder.Languages"
             x:Class="ShoppingReminder.View.PhotoPage">
  <ContentView.Content>
      <StackLayout>
            <local:AdMobView Style="{StaticResource AdBlock}"/>
            <StackLayout Orientation="Horizontal">
                <Label Text="{languages:Translate Text=PurchaseReceiptsFrom}" Style="{DynamicResource Discription}"/>
                <Label Text="{Binding }"/>
            </StackLayout>
            <ListView ItemsSource="{Binding }">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <ViewCell.View>
                                <StackLayout>
                                    <Image/>
                                </StackLayout>
                            </ViewCell.View>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>                
            </ListView>
            <Label x:Name="NoPhotos" Text="{languages:Translate Text=NoCheckPhotosHaveBeenTakenWithThisPurchase}" IsVisible="False"
                   Style="{DynamicResource Discription}"/>
        </StackLayout>
  </ContentView.Content>
</ContentView>